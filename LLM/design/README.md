# Design

How approved requirements will be satisfied. One file per area, named `{area}.md`.

Use `LLM/DESIGN_TEMPLATE.md` for the structure.

Rules:
- Design cannot invent new requirements. If design reveals a missing requirement, update requirements first.
- If you already have authoritative design / architecture docs elsewhere, do not duplicate them here. Reference them from `LLM/SPEC.md`.
